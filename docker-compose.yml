services:
  fe:
    build: .
    container_name: market-lens-fe
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - marketlens-net

networks:
  marketlens-net:
    external: true
